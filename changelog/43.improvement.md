Replaced the stock Python `.gitignore` in a generated feedstock with a short one.
It names only what a feedstock produces: the bundle, the virtual environment, the tool caches,
the rejections a `copier update` leaves behind, and macOS clutter.
