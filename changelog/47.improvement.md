Trimmed the generated README to the dataset itself: what it is, how to build it and how to change it.
The platform and CI narrative now lives only in this template's README, which the generated one links to.
