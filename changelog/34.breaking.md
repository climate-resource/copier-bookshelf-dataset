Made `sdk-version` a required input of the reusable feedstock CI workflow.
Its `upload preview` job also needs the caller to grant `id-token: write`.
A caller that moves to this template version without both fails at startup.
