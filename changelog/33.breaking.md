Replaced the single `Record and validate bundles` CI job and its `bookshelf-bundle` artifact
with the `candidate`, `targets`, `record` and `candidate outcome` jobs and one `bookshelf-bundle-<volume>-<version>` artifact per book.
A branch ruleset that requires the old check name has to be updated after moving to this template version.
