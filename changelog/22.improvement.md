Raised the scaffolded `bookshelf` floor to 1.0.0b2.
A checked-in `path:` input records its bytes from that release, so an earlier one generates a
feedstock that records and validates but cannot publish.
