Trimmed the generated `ruff.toml` and `.pre-commit-config.yaml` from 93 lines to 43.
The ruff config now carries house style only, because ruff reads the Python version and the line length from `pyproject.toml`.
The pre-commit config keeps the hooks that stop a feedstock committing data, keys or Copier rejection files, and drops the ones ruff already covers.
Renovate moves the hook revisions, so the pre-commit.ci autoupdate block is gone.
