[project]
name = "bookshelf-primap-hist-2024"
version = "0.1.0"
description = "Historical national emissions, hyphenated and numbered."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bookshelf[publish]==1.0.0b14",
    "pandas>=2.2",
]

[tool.uv]
exclude-newer = "3 days"
exclude-newer-package = { bookshelf = false }
