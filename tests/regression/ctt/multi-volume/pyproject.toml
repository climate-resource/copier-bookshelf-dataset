[project]
name = "bookshelf-multi-volume"
version = "0.1.0"
description = "Two volumes built from one feedstock."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bookshelf[publish,dataframes]==1.0.0b13",
    "pandas>=2.2",
]

[tool.uv]
exclude-newer = "3 days"
exclude-newer-package = { bookshelf = false }
