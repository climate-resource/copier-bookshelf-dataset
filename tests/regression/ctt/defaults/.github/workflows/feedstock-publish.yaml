name: Feedstock publish

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    uses: climate-resource/copier-bookshelf-dataset/.github/workflows/feedstock-publish.yaml@HEAD
    with:
      api-base-url: ${{ vars.BOOKSHELF_API_BASE_URL || 'https://api.climateresource.com.au/bookshelf' }}
      token-url: ${{ vars.BOOKSHELF_TOKEN_URL }}
    secrets: inherit
