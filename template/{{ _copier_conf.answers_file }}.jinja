# Changes here will be overwritten by Copier; NEVER EDIT MANUALLY
{# These two are never asked but still recorded, because an update must not drop a hand-set value. #}
{{ _copier_answers | combine({"project_url": project_url, "extra_recipes": extra_recipes}) | to_nice_yaml -}}
